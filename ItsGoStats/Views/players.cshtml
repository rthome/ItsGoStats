@inherits Nancy.ViewEngines.Razor.NancyRazorViewBase<dynamic>

@{
    Layout = "_base";
}

<ul>
    @foreach (var player in Model.Players)
    {
        <li>@player.Name (@player.SteamId)</li>
    }
</ul>
